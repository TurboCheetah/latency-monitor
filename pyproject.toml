[tool.poetry]
name = "mtr-probe"
version = "1.0.0"
description = "simple HTTP server that displays MTR results that update every 15m"
authors = ["Turbo <dev@turbo.ooo>"]
license = "GPLv3"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
Flask = "^2.3.2"
celery = { extras = ["redis"], version = "^5.3.1" }
redis = "^4.6.0"
gunicorn = "^21.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
